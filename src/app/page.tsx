import { ExchangeWidget } from "@/components/exchange-widget";

export default function Home() {
    return (
        <main className="w-full flex justify-center px-4 h-screen items-center">
            <ExchangeWidget />
        </main>
    );
}
